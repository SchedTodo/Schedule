# ANTLR Tooling

Schedule pins the official ANTLR generator and TypeScript runtime to 4.13.2.

Regenerate parser sources with:

```powershell
pnpm parser:generate
```

`src/parser/generated` is generated output. Never edit it manually.
